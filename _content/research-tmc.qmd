This work considers the freshness of sensing targets together with the freshness of processed data delivered through multicast and unicast, while also accounting for edge-computation and queuing delays. Results show that jointly scheduling sensing, multicast, and unicast transmissions reduces the total weighted AoI and latency violations across different traffic loads, QoS requirements, and network sizes.
